development:
  clients:
    default:
      database: consumer_api_development
      hosts:
        - localhost:27017
      options:
  driver_options:

test:
  clients:
    default:
      database: consumer_api_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1